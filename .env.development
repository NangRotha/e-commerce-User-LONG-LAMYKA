# Development — Local Backend API URL
# ប្រើពេល `npm run dev` (Vite loads .env.development ដោយស្វ័យប្រវត្តិ)
VITE_API_URL=http://localhost:8000

# WebSocket URL សម្រាប់ real-time updates
VITE_WS_URL=ws://localhost:8000

