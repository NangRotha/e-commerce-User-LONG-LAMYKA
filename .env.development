# Development — Backend API URL
# Set to Render backend so local dev connects to live data immediately
# (To use local backend instead, change to http://localhost:8000)
VITE_API_URL=https://e-commerce-backend-long-lamyka.onrender.com

# WebSocket URL សម្រាប់ real-time updates
VITE_WS_URL=wss://e-commerce-backend-long-lamyka.onrender.com

